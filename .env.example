# Path to the SQLite database file. Defaults to backend/tickets.db if unset.
DATABASE_PATH=./tickets.db

# Port the app listens on. Most PaaS providers (Railway, Render) inject
# this automatically — only needed for local overrides.
PORT=8000
